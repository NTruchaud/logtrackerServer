#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../logcat/bin/logcat" "$@"
  ret=$?
else 
  node  "$basedir/../logcat/bin/logcat" "$@"
  ret=$?
fi
exit $ret
